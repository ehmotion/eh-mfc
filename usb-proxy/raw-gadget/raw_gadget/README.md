Raw Gadget Kernel Module
========================

## Usage

Optionally update:

``` bash
./update.sh
```

Build:
sudo apt-get install linux-headers-`uname -r`
-or-
apt-get install linux-generic

``` bash
make
```

Insmod:

``` bash
./insmod.sh
```
